# ORAM-Integrated PyTorch Training Baseline
# Caden Roberts March 2026

# Core ML
torch>=2.0.0
torchvision>=0.15.0

# ORAM Implementation
pyoram>=0.2.1

# Cryptography (PyORAM backend)
cryptography>=41.0.0
pycryptodome>=3.19.0

# Profiling and Analysis
memory-profiler>=0.61.0
psutil>=5.9.0
matplotlib>=3.7.0
pandas>=2.0.0
seaborn>=0.12.0
numpy>=1.24.0

# Machine Learning
scikit-learn>=1.3.0
xgboost>=2.0.0

# OS-Level Tracing (Linux only, optional)
# For eBPF trace capture: sudo apt install bpfcc-tools python3-bpfcc
# bcc is not pip-installable, must use system package manager
# Fallback: strace (usually pre-installed on Linux)

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
